import { ContainPipePipe } from './contain-pipe.pipe';

describe('ContainPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContainPipePipe();
    expect(pipe).toBeTruthy();
  });
});
